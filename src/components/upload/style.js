import styled from 'styled-components';

export const UploadWrapper = styled.div`
  background-color: var(--white-two);
  text-align: center;
  margin-top: 50px;
  cursor: pointer;
  .ant-upload {
    cursor: pointer;
    width: 100%;
    font-size: 16px;
    font-weight: bold;
    font-stretch: normal;
    font-style: normal;
    line-height: normal;
    letter-spacing: normal;
  }
  .component_upload_button {
    color: var(--dark-green);
    padding: 20px 0;
    width: 100%;
  }
`;
